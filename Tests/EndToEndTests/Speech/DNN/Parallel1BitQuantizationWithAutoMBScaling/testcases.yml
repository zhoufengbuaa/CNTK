dataDir: ../../Data
tags:
     # On Windows don't run CPU (too many flavors, tolerance too strict)
     - bvt-s (build_sku == '1bitsgd') and (((flavor == 'release') and (device == 'gpu')) if (os == 'windows') else ((flavor == 'debug') ^ (device == 'cpu')))
     - nightly-s (build_sku == '1bitsgd') and ((os == 'linux') or (device == 'gpu'))

testCases:
  Must train epochs in exactly same order and parameters for each MPI Rank:
    patterns:
      - ^MPI Rank {{integer}}
      - Starting Epoch {{integer}}
      - learning rate per sample = {{float}}
      - momentum = {{float}}

  Epochs must be finished with expected results for each MPI Rank:
    patterns:
      - ^MPI Rank {{integer}}
      - Finished Epoch[{{integer}} of {{integer}}]
      - CrossEntropyWithSoftmax = {{float,tolerance=0%}}
      - EvalClassificationError = {{float,tolerance=0%}}
      - learningRatePerSample = {{float,tolerance=0.001%}}

  Per-minibatch training results must match for each MPI Rank:
    patterns:
      - ^MPI Rank {{integer}}
      - Epoch[{{integer}} of {{integer}}]-Minibatch[{{integer}}-{{integer}}
      - " * {{integer}}; "
      - CrossEntropyWithSoftmax = {{float,tolerance=0%}}
      - EvalClassificationError = {{float,tolerance=0%}}

  DataParallelSGD training parameters must match for each MPI Rank:
    patterns:
      - ^MPI Rank {{integer}}
      - Starting minibatch loop
      - DataParallelSGD training
      - myRank = {{integer}}
      - numNodes = 2
      - numGradientBits = 2
      - distributed reading is ENABLED
